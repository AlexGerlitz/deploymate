export default function RegisterPage() {
  return (
    <main className="starterShell">
      <div className="starterWrap">
        <section className="starterCard">
          <p className="starterEyebrow">Auth Starter</p>
          <h1>Create account</h1>
          <p>This starter page exists so the next project begins with a visible acquisition path, not an empty route.</p>
          <div className="starterActions">
            <a className="starterButton starterButtonPrimary" href="/app">Open app shell</a>
            <a className="starterButton starterButtonSecondary" href="/login">Back to login</a>
          </div>
        </section>
      </div>
    </main>
  );
}
